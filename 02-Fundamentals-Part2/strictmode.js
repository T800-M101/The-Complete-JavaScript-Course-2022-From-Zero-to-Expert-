'use strict';

// strict mode forbide us to do certain things and warns us through visible errors in certain situatios

let hasDriversLicense = false;
const passTest = true;

if(passTest) hasDriverLicense = true;
if(hasDriversLicense) console.log('I can drive');

const interface = 'Audio';

const private = 534;
